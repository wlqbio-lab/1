# Down 100 Floors (是男人就下100层)

A lightweight 终端小游戏，复刻经典的“是男人就下100层”。通过左右移动角色，穿过迎面而来的地板缺口，成功下落 100 层即可获胜。

## 运行方式

```bash
python3 game.py
```

进入游戏后：

- 使用方向键 ← / → 或 A / D 键左右移动角色；
- 角色要站在空隙处才能顺利穿过地板；
- 成功通过 100 层即胜利，失败后可以按 `R` 重新开始，`Q` 退出游戏。

> 注意：这是一个基于 `curses` 的终端游戏，请在支持 ANSI 终端控制的环境中运行。
